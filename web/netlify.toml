[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Proxy /api/* requests to the Render backend
[[redirects]]
  from = "/api/*"
  to = "https://airport-solar-api2.onrender.com/api/:splat"
  status = 200
  force = true
